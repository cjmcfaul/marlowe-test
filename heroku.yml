build:
  docker:
    web: ./compose/production/django/Dockerfile
  config:
    DJANGO_SETTINGS_MODULE: config.settings.production
release:
  image: web
  command:
    - python manage.py migrate --noinput
    - python manage.py collectstatic --noinput